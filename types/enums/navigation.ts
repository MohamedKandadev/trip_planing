export enum ROUTE {
  ONBOARDING = 'onboarding',
  SIGNIN = 'signin',
  SIGNUP = 'signup',
  TRIP_INFO = 'trip info',
  DESTINATIONS = 'destinations',
  TABS = 'TABS',
}

export enum TAB_ROUTE {
  HOME = 'home',
  PROFILE = 'profile',
  NEW_TRIP = 'new trip',
}
