import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const TripInfoForm = (props: Props) => {
  return (
    <View>
      <Text>TripInfoForm</Text>
    </View>
  );
};

export default TripInfoForm;

const styles = StyleSheet.create({});
